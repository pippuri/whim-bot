{
  "description": "This is a sample input for get-point-pricing rule",
  "schema": {
    "type": "object",
    "properties": {
      "currency": {
        "type": "string",
        "enum": ["EUR"]
      }
    },
    "required": ["currency"]
  }
}

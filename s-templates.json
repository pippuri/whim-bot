{
  "environment": {
    "SERVERLESS_STAGE": "${stage}",
    "SERVERLESS_IAM_ROLE_ARN_LAMBDA": "${iamRoleArnLambda}",
    "AWS_REGION": "${AWS_REGION}",
    "IOT_ENDPOINT": "${IOT_ENDPOINT}",
    "SMS_CODE_SECRET": "${SMS_CODE_SECRET}",
    "JWT_SECRET": "${JWT_SECRET}",
    "ZENDESK_JWT_SECRET": "${ZENDESK_JWT_SECRET}",
    "TRIPGO_API_KEY": "${TRIPGO_API_KEY}",
    "HERE_APP_ID": "${HERE_APP_ID}",
    "HERE_APP_CODE": "${HERE_APP_CODE}",
    "TWILIO_ACCOUNT_SID": "${TWILIO_ACCOUNT_SID}",
    "TWILIO_ACCOUNT_TOKEN": "${TWILIO_ACCOUNT_TOKEN}",
    "TWILIO_FROM_SID": "${TWILIO_FROM_SID}",
    "WWW_BASE_URL": "${WWW_BASE_URL}",
    "COGNITO_POOL_ID": "${COGNITO_POOL_ID}",
    "COGNITO_DEVELOPER_PROVIDER": "${COGNITO_DEVELOPER_PROVIDER}",
    "COGNITO_PROFILE_DATASET": "${COGNITO_PROFILE_DATASET}",
    "COGNITO_USER_DEVICES_DATASET": "${COGNITO_USER_DEVICES_DATASET}",
    "GOOGLE_API_KEY": "${GOOGLE_API_KEY}",
    "CHARGEBEE_API_KEY": "${CHARGEBEE_API_KEY}",
    "CHARGEBEE_SITE": "${CHARGEBEE_SITE}",
    "MAAS_SIGNING_SECRET": "${MAAS_SIGNING_SECRET}",
    "MAAS_PGHOST": "${MAAS_PGHOST}",
    "MAAS_PGUSER": "${MAAS_PGUSER}",
    "MAAS_PGPASSWORD": "${MAAS_PGPASSWORD}",
    "MAAS_PGPORT": "${MAAS_PGPORT}",
    "MAAS_PGDATABASE": "${MAAS_PGDATABASE}",
    "APNS_ARN": "${APNS_ARN}",
    "APNS_ARN_SANDBOX": "${APNS_ARN_SANDBOX}",
    "KNEX_DEBUG": "${KNEX_DEBUG}",
    "TSP_DATASET_PATH": "${TSP_DATASET_PATH}",
    "GCM_ARN": "${GCM_ARN}"
  }
}

{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "https://api.dev.maas.global/v1/autocomplete/query",
  "description": "Lambda input schema for geolocations.",
  "definitions": {
    "autocomplete-core": {
      "type": "object",
      "properties": {
        "name": { "type": "string", "minLength": 5, "maxLength": 128 },
        "count": { "type": "integer", "minimum": 1, "maximum": 100, "default": 5 }
      },
      "required": [ "name" ]
    }
  },
  "anyOf": [
    {
      "$ref": "#/definitions/autocomplete-core",
      "lat": { "type": "number", "minimum": -90, "maximum": 90 },
      "lon": { "type": "number", "minimum": -180, "maximum": 180 },
      "required": [ "lat", "lon" ],
      "inject": {
        "hint": "latlon",
        "radius": 50
      }
    },
    {
      "$ref": "#/definitions/autocomplete-core",
      "inject": {
        "hint": "none"
      }
    }
  ]
}